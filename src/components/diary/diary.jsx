import React from 'react';
import { sliceEvents, createPlugin } from '@fullcalendar/common';

const Diray = (props) => {

    let segs = sliceEvents(props, true);

    return(
        <>
            
        </>
    )
}

export default Diray;